{
  "name": "human-review-chatgpt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Visual HTML review and editing inside ChatGPT using MCP Apps UI.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/store.js && node --check src/sanitize.js && node --check public/review-widget.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.0.1",
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  }
}
